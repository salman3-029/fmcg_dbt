version: 2

models:
  - name: stg_customers
    columns:
      - name: customer_code
        tests:
          - unique
          - not_null
      - name: city
        tests:
          - not_null
          - accepted_values: 
              arguments: 
                values: ['Bengaluru', 'Hyderabad', 'New Delhi', 'Unknown']

